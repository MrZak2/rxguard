# openFDA
# (Optional but recommended: increases quota & reduces rate-limit headaches.)
OPENFDA_API_KEY=

# Firebase Admin
# If running locally, prefer setting GOOGLE_APPLICATION_CREDENTIALS.
# Alternatively you can pass the entire service account JSON (not recommended to commit).
FIREBASE_SERVICE_ACCOUNT_KEY_JSON=

# Storage bucket (required for large-label fallbacks). Example: <project-id>.appspot.com
FIREBASE_STORAGE_BUCKET=

# Baseline model endpoint for “naked” output (demo)
# Pick a provider:
# - openai_compat (vLLM/TGI / OpenAI-compatible server): base URL like http://localhost:8000
# - ollama (native): base URL like http://localhost:11434
RXGUARD_LLM_PROVIDER=openai_compat
RXGUARD_LLM_BASE_URL=
RXGUARD_LLM_API_KEY=

# Model names for your server
RXGUARD_MISTRAL_MODEL=mistral-7b
RXGUARD_LLAMA_MODEL=llama3-8b
